export interface ICategoriaModel{ //Modelo de las categorias. esto se hace para tener un mejor control de los datos que vienen en la api

      codigo : number;
      estado :String;
      limInferior : number;
      limSuperior :number;
}